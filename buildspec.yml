version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.8
  build:
    commands:
      - sam build 
  post_build:
    commands:
      - sam deploy --stack-name sam-test-v2 --config-env master
# artifacts:
#   files:
#     - template.yaml
